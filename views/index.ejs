<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <!-- Compiled and minified CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.100.2/css/materialize.min.css">
    
    
    <title>My App</title>
</head>
<style>
    .quote--name, .quote--quote{
        text-transform: capitalize;
    }
</style>
<body>
 <div class="container">
     <div>
            <p>May Node and Express be with you!</p>
            <div class="card">
                <div class="card-content">
                    <form action="/quotes" method="POST">
                        <input type="text" placeholder="Name" name="name">
                        <input type="text" placeholder="Quote" name="quote">
                        <button class="btn waves-effect" type="submit">Submit</button>
                    </form>
                </div>
            </div>
            <table>
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Quote</th>
                        <th>Actions</th>
                    </tr>
                </thead>
            
                <tbody>
                    <% for (var i=0; i< quotes.length; i++) {%>
                    <tr>
                        <td><%= quotes[i].name %></td>
                        <td><%= quotes[i].quote %></td>
                        <td>
                            <button class="delete-btn btn waves-effect" onclick="deleteComment('<%= quotes[i]._id %>')">Delete</button>
                        </td>
                    </tr>
                    <% } %>
                    
                </tbody>
            </table>
            <!-- <ul class="quotes">
                <% for (var i=0; i< quotes.length; i++) {%>
                    <li class="quote">
                        <span class="quote--name">
                            <%= quotes[i].name %> :
                        </span>
                        <span class="quote--quote">
                            <%= quotes[i].quote %>
                        </span>
                    </li>
                    <% } %>
            </ul> -->
            
            
            
            <div>
                <h5>Replace last quote written by Master Yoda with a quote written by Darth Vadar</h5>
                <button class="btn waves-effect" id="update">Darth Vadar invades!</button>
            </div>
            
            <div>
                <h5>Delete Darth Vadar's first quote last quote </h5>
                <button class="btn waves-effect" id="delete">Delete first Darth Vadar quote</button>
            </div>
     </div>
 </div>
</body>
<script src="main.js"></script>
<script type="text/javascript" src="https://code.jquery.com/jquery-3.2.1.min.js"></script>
<!-- Compiled and minified JavaScript -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.100.2/js/materialize.min.js"></script>
</html>

